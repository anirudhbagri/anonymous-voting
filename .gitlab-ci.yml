image: cirrusci/flutter:stable

stages:
  - build
  
build:
  stage: build
  script:
    - cd  client
    - flutter build apk
    - ls
  artifacts:
    paths:
    - build/
    expire_in: 1 year
  tags:
    - shared